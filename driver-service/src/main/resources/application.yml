server:
  port: 5003

spring:
  profiles:
    active: dev
  application:
    name: driver-service

  jpa:
    hibernate:
      ddl-auto: none

management:
  endpoints:
    web:
      exposure:
        include: "*"